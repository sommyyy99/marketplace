// Single source of truth for Sommygo's fees. Mirrors src/data/products.ts so
// the amount shown in the basket and the amount actually charged always match.
export const DELIVERY_FEE_PER_VENDOR = 2500;
export const SERVICE_FEE = 700;
